# Copy this file to a new file called ".env" and paste your real key in there.
# The .env file is gitignored, so it is never committed, shared, or seen by Claude.
ANTHROPIC_API_KEY=sk-ant-your-key-goes-here
